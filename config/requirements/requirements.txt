# YourDaddy Assistant - Unified Requirements
# ==========================================
# To install specific subsets:
#   pip install -r config/requirements/base.txt    (Web API & Security only)
#   pip install -r config/requirements/voice.txt   (Voice & Speech only)
#   pip install -r config/requirements/ml.txt      (ML & AI features)
#   pip install -r config/requirements/dev.txt     (Dev & Testing tools)
#
# To install all core features:
#   pip install -r requirements.txt

-r base.txt
-r voice.txt
-r ml.txt